// The same Suspense boundary the real screens get from (app)/loading.tsx, so
// this route streams and hydrates its page segment separately from the shell.
export { default } from '@/app/(app)/loading'
